import { defineConfig, devices } from '@playwright/test';

/**
 * Generated by qa-ai. Chromium desktop only (v1 — multi-browser is out of
 * scope). The JSON reporter's results.json is the machine handoff consumed by
 * `qa-ai heal`; the HTML report is the human-facing output.
 *
 * `testIdAttribute` is set to `data-test` to match the reference target
 * (saucedemo). Change it if your app uses `data-testid`/`data-cy`/etc.
 */
export default defineConfig({
  testDir: './tests',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 1 : 0,
  reporter: [
    ['html', { open: 'never' }],
    ['json', { outputFile: 'results.json' }],
  ],
  use: {
    baseURL: '{{baseUrl}}',
    trace: 'on-first-retry',
    testIdAttribute: 'data-test',
  },
  projects: [{ name: 'chromium', use: { ...devices['Desktop Chrome'] } }],
});
